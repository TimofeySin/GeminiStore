<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GeminiStore" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/twins</item>
        <item name="colorPrimaryVariant">@color/twins</item>
        <item name="colorOnPrimary">@color/twins</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/twins</item>
        <item name="colorSecondaryVariant">@color/twins</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.GeminiStore.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.GeminiStore.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.GeminiStore.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>